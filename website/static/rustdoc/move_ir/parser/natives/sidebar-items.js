initSidebarItems({"enum":[["NativeFunction","Set of functions that could theoritically be implemented in the language (if the language) was more expressive. But have a native implementation. Most of these are for collections or natively implemented data types Some of these might be implementable in the language (with maybe the addition of a few primitives), but they are kept native for a smaller core language and a more efficient implementation"]]});